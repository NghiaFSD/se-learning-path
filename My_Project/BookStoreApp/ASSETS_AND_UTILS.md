# Assets & Utils - Cấu trúc và mô tả\n\n## 📁 Assets Directory Structure\n\nProject sử dụng cấu trúc assets mô-đun với các tệp CSS, JavaScript và hình ảnh được tổ chức rõ ràng.\n\n### web/assets/\n\n`\nassets/\n├── css/\n│   ├── style.css           (1,200 lines) - CSS tùy chỉnh chính\n│   ├── responsive.css      (400 lines) - Media queries cho mobile/tablet/desktop\n│   └── variables.css       (250 lines) - CSS variables & theming\n│\n├── js/\n│   ├── app.js              (300 lines) - Hàm tiện ích toàn cầu\n│   ├── validation.js       (350 lines) - Validation form client-side\n│   ├── cart.js             (400 lines) - Logic giỏ hàng\n│   └── [custom].js         - Tệp JavaScript tùy chỉnh khác\n│\n└── images/\n    ├── logo.svg/logo.png   - Logo ứng dụng\n    ├── favicon.ico         - Icon trên tab browser\n    ├── placeholder.png     - Hình ảnh mặc định\n    ├── cover-*.jpg         - Bìa sách\n    └── icons/              - Thư mục chứa SVG icons\n        ├── cart.svg\n        ├── user.svg\n        ├── search.svg\n        └── ...\n`\n\n---\n\n## 🎨 CSS Files\n\n### style.css (2,000+ lines)\n**Nội dung chính:**\n- Biến CSS toàn cầu (màu sắc, bóng, border-radius)\n- Styling navbar, book cards, forms, buttons\n- Badge, alert, table styles\n- Footer styling\n- Custom animations\n- Scrollbar styling\n\n**Lớp chính:**\n`css\n:root {}\n/* Global, Navbar, Cards, Forms, Buttons, Badges, Alerts, Tables, Footer */\n`\n\n**Sử dụng:**\n`html\n<link rel=\"stylesheet\" href=\"assets/css/style.css\">\n`\n\n---\n\n### responsive.css (600+ lines)\n**Nội dung chính:**\n- Breakpoints: <576px, 576px+, 768px+, 992px+, 1200px+, 1400px+\n- Landscape mode (mobile)\n- Print styles\n- Accessibility (high contrast, reduced motion)\n- Dark mode support (prefers-color-scheme)\n\n**Breakpoints (Bootstrap standard):**\n- **xs** - Extra small (<576px) - Phones\n- **sm** - Small (≥576px) - Large phones\n- **md** - Medium (≥768px) - Tablets\n- **lg** - Large (≥992px) - Desktops\n- **xl** - Extra large (≥1200px) - Large desktops\n- **xxl** - Extra extra large (≥1400px) - Ultra-wide\n\n**Sử dụng:**\n`html\n<link rel=\"stylesheet\" href=\"assets/css/responsive.css\">\n`\n\n---\n\n### variables.css (250+ lines)\n**Nội dung chính:**\n- CSS Custom Properties (CSS Variables)\n- Màu sắc (primary, secondary, success, danger, warning, info)\n- Typography (font families, sizes, weights)\n- Spacing scale (1rem - 20rem)\n- Border radius values\n- Shadow definitions\n- Transitions\n- Z-index scale\n- Utility classes\n\n**Ví dụ biến:**\n`css\n:root {\n    --primary: #2c3e50;\n    --secondary: #3498db;\n    --spacing-4: 1rem;\n    --radius-lg: 0.75rem;\n    --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1);\n}\n`\n\n**Sử dụng trong CSS:**\n`css\n.btn {\n    background: var(--primary);\n    padding: var(--spacing-4);\n    border-radius: var(--radius-lg);\n    box-shadow: var(--shadow-md);\n}\n`\n\n**Sử dụng:**\n`html\n<link rel=\"stylesheet\" href=\"assets/css/variables.css\">\n`\n\n---\n\n## 📜 JavaScript Files\n\n### app.js (300+ lines)\n**Lớp chính:**\n- Document ready initialization\n- Bootstrap components setup\n- Toast notifications\n- Currency & date formatting\n- Debounce function\n- User authentication checks\n\n**Hàm chính:**\n`javascript\ninitializeBootstrapComponents()      // Tooltips, Popovers\nshowToast(message, type, duration)   // Thông báo tự động ẩn\nformatCurrency(amount)                // Định dạng VND\nformatDate(date)                      // Định dạng ngày\ndebounce(func, wait)                  // Giới hạn tần suất gọi hàm\nconfirmAction(message, callback)      // Xác nhận hành động\ndisableButtonTemporarily()            // Ngăn double-submit\nisLoggedIn()                          // Kiểm tra đăng nhập\nrequireLogin()                        // Redirect nếu chưa login\n`\n\n**Sử dụng:**\n`html\n<script src=\"assets/js/app.js\"></script>\n<script>\n    BookStoreApp.showToast('Thành công!', 'success');\n    console.log(BookStoreApp.formatCurrency(100000));\n</script>\n`\n\n---\n\n### validation.js (350+ lines)\n**Lớp chính:**\n- Email, username, password validation\n- Phone, URL, price, quantity validation\n- Form validation & real-time validation\n- Input sanitization\n- Error message display\n\n**Hàm chính:**\n`javascript\nisValidEmail(email)                   // Kiểm tra email\nvalidatePasswordStrength(password)    // Độ mạnh mật khẩu\nisValidUsername(username)             // Kiểm tra username\nisValidPhoneNumber(phone)             // Kiểm tra SĐT VN\nisValidPrice(price)                   // Kiểm tra giá\nisValidQuantity(quantity)             // Kiểm tra số lượng\nsanitizeInput(input)                  // Ngăn XSS\nvalidateForm(form)                    // Validate toàn bộ form\naddRealTimeValidation(form)           // Validation khi blur/type\n`\n\n**Sử dụng:**\n`html\n<script src=\"assets/js/validation.js\"></script>\n<form id=\"registerForm\">\n    <input type=\"email\" name=\"email\" required>\n    <input type=\"password\" name=\"password\" required>\n    <button type=\"submit\">Đăng ký</button>\n</form>\n<script>\n    const form = document.getElementById('registerForm');\n    ValidationUtil.addRealTimeValidation(form);\n</script>\n`\n\n---\n\n### cart.js (400+ lines)\n**Lớp chính:**\n- Quản lý giỏ hàng UI\n- Cập nhật số lượng, xóa item\n- Tính tổng tiền\n- Thêm sản phẩm với feedback\n- Áp dụng coupon\n\n**Hàm chính:**\n`javascript\nupdateCartDisplay(itemCount, totalPrice)    // Cập nhật navbar\naddToCartWithFeedback(bookId, bookTitle)    // Thêm với animation\nupdateCartItemQuantity(itemId, qty)         // Thay đổi số lượng\nremoveCartItem(itemId)                      // Xóa item\ncalculateCartTotal()                        // Tính tổng\nshowEmptyCartMessage()                      // Giỏ trống\nclearCart()                                 // Xóa toàn bộ\nappplyCoupon(couponCode)                    // Mã giảm giá\n`\n\n**Sử dụng:**\n`html\n<script src=\"assets/js/cart.js\"></script>\n<button onclick=\"CartUtil.addToCartWithFeedback(5, 'Clean Code')\">Add to Cart</button>\n`\n\n---\n\n## 🛠️ Utility Classes (Java)\n\n### src/java/utils/\n\n`\nutils/\n├── ValidationUtil.java      (350 lines) - Kiểm tra dữ liệu\n├── DateUtil.java            (300 lines) - Xử lý ngày tháng\n├── StringUtil.java          (350 lines) - Xử lý chuỗi\n├── NumberUtil.java          (300 lines) - Định dạng số tiền\n├── SecurityUtil.java        (300 lines) - Bảo mật & mã hóa\n└── PaginationUtil.java      (250 lines) - Phân trang\n`\n\n---\n\n### ValidationUtil.java (350 lines)\n**Hàm chính:**\n`java\nisValidEmail(String email)                  // Kiểm tra email\nisValidUsername(String username)            // Username format\nisValidPassword(String password)            // Mật khẩu an toàn\nisValidPhoneNumber(String phone)            // SĐT Việt Nam\nisValidUrl(String url)                      // URL format\nisNumeric(String value)                     // Là số?\nisNotEmpty(String input)                    // Không rỗng?\nsanitizeInput(String input)                 // Chống XSS\nsanitizeSqlInput(String input)              // Chống SQL injection\nlimitLength(String input, int maxLength)    // Giới hạn độ dài\nisValidPrice(String price)                  // Giá hợp lệ\nisValidQuantity(String quantity)            // Số lượng 1-999\nisValidBookId(String bookId)                // ID sách hợp lệ\nisValidCategoryId(String categoryId)        // ID danh mục hợp lệ\n`\n\n**Sử dụng:**\n`java\nif (ValidationUtil.isValidEmail(email)) {\n    // Process email\n}\n\nif (ValidationUtil.isValidPrice(price)) {\n    // Add to cart\n}\n`\n\n---\n\n### DateUtil.java (300 lines)\n**Hàm chính:**\n`java\ngetCurrentDate()                            // Ngày hiện tại\ngetCurrentDateTime()                        // Ngày giờ hiện tại\nformatDate(LocalDate date)                  // Định dạng dd/MM/yyyy\nformatDateTime(LocalDateTime dt)            // Định dạng dd/MM/yyyy HH:mm:ss\nparseDate(String dateString)                // Parse từ chuỗi\nisToday(LocalDate date)                     // Hôm nay?\nisPast(LocalDate date)                      // Quá khứ?\nisFuture(LocalDate date)                    // Tương lai?\ndaysBetween(LocalDate from, LocalDate to)   // Số ngày chênh lệch\naddDays(LocalDate date, int days)           // Cộng ngày\nsubtractDays(LocalDate date, int days)      // Trừ ngày\ngetRelativeTime(LocalDateTime dt)           // \"2 giờ trước\"\n`\n\n**Sử dụng:**\n`java\nString today = DateUtil.formatDate(DateUtil.getCurrentDate());\nlong daysDiff = DateUtil.daysBetween(startDate, endDate);\n`\n\n---\n\n### StringUtil.java (350 lines)\n**Hàm chính:**\n`java\nisEmpty(String str)                         // Rỗng?\ntruncate(String str, int maxLength)         // Cắt với \"...\"\ncapitalize(String str)                      // Viết hoa chữ cái đầu\ntoTitleCase(String str)                     // Mỗi từ viết hoa\nremoveWhitespace(String str)                // Xóa khoảng trắng\nnormalizeSpaces(String str)                 // Normalize spaces\nreverse(String str)                         // Đảo ngược chuỗi\nextractNumbers(String str)                  // Chỉ lấy số\nextractLetters(String str)                  // Chỉ lấy chữ\ntoSlug(String str)                          // URL slug\nmaskSensitiveData(String str, int visible)  // Che dữ liệu nhạy cảm\njoin(String separator, String... strings)   // Nối chuỗi\n`\n\n**Sử dụng:**\n`java\nString truncated = StringUtil.truncate(bookTitle, 50);\nString slug = StringUtil.toSlug(\"Harry Potter And The Philosopher's Stone\");\nString masked = StringUtil.maskSensitiveData(email, 3); // \"usr***@example.com\"\n`\n\n---\n\n### NumberUtil.java (300 lines)\n**Hàm chính:**\n`java\nformatCurrency(double amount)               // Định dạng VND\nformatNumber(long number)                   // Định dạng số với dấu phẩy\nround(double value, int places)             // Làm tròn\ncalculatePercentage(double v, double t)    // % = (v/t)*100\ncalculateDiscount(double price, double %)   // Tính tiền giảm\ncalculateFinalPrice(double price, double %) // Giá sau giảm\nformatShortNumber(long number)              // \"1.5K\", \"2.3M\"\ninRange(double n, double min, double max)   // Trong khoảng?\nparseDouble(String value, double def)       // Parse an toàn\nparseInt(String value, int def)             // Parse an toàn\nisEven(int number)                          // Số chẵn?\nisOdd(int number)                           // Số lẻ?\n`\n\n**Sử dụng:**\n`java\nString formattedPrice = NumberUtil.formatCurrency(150000); // \"150,000đ\"\ndouble finalPrice = NumberUtil.calculateFinalPrice(100000, 10); // 90000\n`\n\n---\n\n### SecurityUtil.java (300 lines)\n**Hàm chính:**\n`java\ngenerateSalt()                              // Random salt\nhashSHA256(String input)                    // SHA-256 hash\nhashSHA256WithSalt(String input, String s)  // Hash + salt\nhashMD5(String input)                       // MD5 hash (cũ)\ngenerateToken()                             // Random token 32 bytes\ngenerateToken(int length)                   // Token tùy chiều dài\ngenerateOTP()                               // OTP 6 chữ số\ngenerateOTP(int length)                     // OTP tùy chiều dài\nescapeHtml(String input)                    // XSS prevention\nescapeJavaScript(String input)              // JS XSS prevention\nescape Sql(String input)                    // SQL injection prevention\nencodeBase64(String input)                  // Base64 encode\ndecodeBase64(String encoded)                // Base64 decode\nisPasswordComplex(String password)          // Kiểm tra độ phức tạp\n`\n\n**Sử dụng:**\n`java\nString salt = SecurityUtil.generateSalt();\nString hashed = SecurityUtil.hashSHA256WithSalt(password, salt);\nString token = SecurityUtil.generateToken(32);\nString otp = SecurityUtil.generateOTP(6);\n`\n\n---\n\n### PaginationUtil.java (250 lines)\n**Hàm chính:**\n`java\nnew PaginationUtil(page, pageSize, total)   // Constructor\ngetCurrentPage()                            // Trang hiện tại\ngetPageSize()                               // Số bản ghi/trang\ngetTotalPages()                             // Tổng trang\ngetStartIndex()                             // Index bắt đầu\ngetEndIndex()                               // Index kết thúc\nhasPreviousPage()                           // Có trang trước?\nhasNextPage()                               // Có trang sau?\ngetPreviousPage()                           // Số trang trước\ngetNextPage()                               // Số trang sau\ngetPageNumbers(int maxPages)                // Số trang hiển thị\ngetOffset()                                 // SQL OFFSET\ngetLimit()                                  // SQL LIMIT\ngetInfo()                                   // Text thông tin: \"Page 1 of 5...\"\ngetHtmlPagination(String baseUrl)           // HTML pagination Bootstrap\nbuildPageUrl(String base, int page)         // Build URL với page param\n`\n\n**Sử dụng:**\n`java\nPaginationUtil pagination = new PaginationUtil(\n    request.getParameter(\"page\"),\n    10,\n    totalBooks\n);\n\nList<Book> books = bookDAO.getBooks(\n    pagination.getOffset(),\n    pagination.getLimit()\n);\n\n// JSP\n<%= pagination.getInfo() %>\n<%= pagination.getHtmlPagination(\"home?category=1\") %>\n`\n\n---\n\n## 📊 Integration Example\n\n### Trong Servlet:\n`java\npublic void doGet(HttpServletRequest req, HttpServletResponse res) \n        throws ServletException, IOException {\n    \n    // Validation\n    String email = req.getParameter(\"email\");\n    if (!ValidationUtil.isValidEmail(email)) {\n        req.setAttribute(\"error\", \"Email không hợp lệ\");\n        req.getRequestDispatcher(\"register.jsp\").forward(req, res);\n        return;\n    }\n    \n    // Pagination\n    int page = NumberUtil.parseInt(req.getParameter(\"page\"), 1);\n    PaginationUtil pagination = new PaginationUtil(page, 10, totalBooks);\n    \n    // Security\n    String token = SecurityUtil.generateToken();\n    req.getSession().setAttribute(\"csrfToken\", token);\n    \n    // Date\n    String createdDate = DateUtil.formatDate(DateUtil.getCurrentDate());\n    \n    // String\n    String slug = StringUtil.toSlug(bookTitle);\n    String truncated = StringUtil.truncate(description, 100);\n}\n`\n\n### Trong JSP:\n`html\n<%@ page import=\"utils.*\" %>\n\n<h1><%= StringUtil.toTitleCase(bookTitle) %></h1>\n<p><%= StringUtil.truncate(description, 150) %></p>\n\n<p class=\"price\">\n    <%= NumberUtil.formatCurrency(bookPrice) %>\n</p>\n\n<p class=\"created\">\n    <%= DateUtil.formatDate(createdDate) %>\n</p>\n\n<%= pagination.getHtmlPagination(\"home?category=1\") %>\n\n<script src=\"assets/js/validation.js\"></script>\n<script>\n    ValidationUtil.addRealTimeValidation(document.getElementById('registerForm'));\n</script>\n`\n\n---\n\n## 🎯 Best Practices\n\n✅ **Luôn sử dụng Utils thay vì hardcode:**\n`java\n// ✅ Đúng\nString formatted = NumberUtil.formatCurrency(price);\n\n// ❌ Sai\nString formatted = String.format(\"%,.0f\", price) + \"đ\";\n`\n\n✅ **Validation trên cả Client & Server:**\n`java\n// Server-side (Java)\nif (!ValidationUtil.isValidPrice(price)) {\n    // Error\n}\n\n// Client-side (JavaScript)\nif (!ValidationUtil.isValidPrice(price)) {\n    // Error\n}\n`\n\n✅ **Luôn sanitize input:**\n`java\nString input = request.getParameter(\"title\");\ninput = ValidationUtil.sanitizeInput(input);\ninput = ValidationUtil.sanitizeSqlInput(input);\n`\n\n✅ **Sử dụng prepared statement + sanitization:**\n`java\nString query = \"SELECT * FROM Book WHERE title LIKE ?\";\nps.setString(1, \"%\" + cleanInput + \"%\");\n`\n\n---\n\n## 📦 Loading Assets trong JSP\n\n`html\n<!DOCTYPE html>\n<html>\n<head>\n    <!-- Bootstrap CDN -->\n    <link href=\"https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css\" rel=\"stylesheet\">\n    \n    <!-- Custom CSS -->\n    <link rel=\"stylesheet\" href=\"assets/css/variables.css\">\n    <link rel=\"stylesheet\" href=\"assets/css/style.css\">\n    <link rel=\"stylesheet\" href=\"assets/css/responsive.css\">\n</head>\n<body>\n    <!-- Content -->\n    \n    <!-- Bootstrap JS -->\n    <script src=\"https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js\"></script>\n    \n    <!-- Custom JS -->\n    <script src=\"assets/js/app.js\"></script>\n    <script src=\"assets/js/validation.js\"></script>\n    <script src=\"assets/js/cart.js\"></script>\n</body>\n</html>\n`\n\n---\n\nTất cả utils đã được tối ưu hóa cho hiệu suất và bảo mật.\n
